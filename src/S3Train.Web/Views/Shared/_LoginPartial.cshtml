@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        @*<ul class="account">
                <li>
                    @Html.ActionLink(User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
                </li>
                <a href="javascript:document.getElementById('logoutForm').submit()">Log off</a>
            </ul>*@
        <div class="header-bottom-right">
            <div class="account">
                <div class="nav-no-collapse header-nav">
                    <ul class="nav pull-right">
                        <li class="dropdown">
                            <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
                                <span> </span>@User.Identity.GetUserName()<img src="~/Content/images/b-arrow.png" />
                            </a>
                            <ul class="dropdown-menu">
                                <li>@Html.ActionLink("Profile", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
                                <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a>
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
            <div class="cart"><a href="@Url.Action("MyCart", "Cart", null)" ><span> </span>CART</a></div>
        </div>
    }
}
else
{
    <div class="header-bottom-right">
        <div class="account">
            <a href="#" data-toggle="modal" data-target="#exampleModal"><span> </span>YOUR ACCOUNT</a>
        </div>
        <div id='loginForm'>
        </div>
        
        <div>
            <div id='registerForm'>
            </div>
            <ul class="login">
                <li>
                    <a href="#" data-toggle="modal" data-target="#exampleModal"><span> </span>LOGIN</a>

                </li> |
                <li><a href="#" data-toggle="modal" data-target="#exampleModal2">SIGNUP</a></li>
            </ul>
        </div>
        <div class="cart"><a href="@Url.Action("MyCart", "Cart", null)" data-toggle="modal" data-target="#exampleModal2"><span> </span>CART</a></div>
        <div class="clearfix"> </div>
    </div>

}
